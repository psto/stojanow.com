---
import AppTitle from "~/components/AppTitle.astro";
import BaseLayout from "~/layouts/BaseLayout.astro";
import ExternalLink from "~/components/ExternalLink.astro";
import site from "~/config/site";
import TheDove from "~/components/TheDove.astro";

const { author, substackUrl, twitterUsername } = site;

const seoProps = {
  description: `Contact ${author} by email or twitter, subscribe to the newsletter or the RSS feed.`,
  id: "contact",
  title: "Contact",
};
---

<BaseLayout {...seoProps}>
  <div class="relative px-4 pt-16 pb-20 sm:px-6 lg:px-8 lg:pt-24 lg:pb-28">
    <div
      class="prose dark:prose-dark lg:prose-lg mx-auto max-w-2xl px-4 text-center md:pt-0"
    >
      <AppTitle>{seoProps.title}</AppTitle>
      <hr />
      <div class="flex justify-center">
        <TheDove />
      </div>
    </div>
    <div class="prose dark:prose-dark mx-auto mt-12 max-w-2xl text-center">
      <p>
        <strong>piotr</strong><span class="text-zinc-600 dark:text-zinc-400"
          >&</span
        ><strong>stojanow@gmail.com</strong>
        <span class="text-zinc-600 dark:text-zinc-400">(without the &)</span> 📧
        <br />
        Get in touch on
        <ExternalLink
          href={`https://twitter.com/${twitterUsername}`}
          className="filter hover:underline"
        >
          twitter 🐦
        </ExternalLink>
        <br />
        <ExternalLink href={`${substackUrl}`} className="hover:underline">
          Subscribe 🔔
        </ExternalLink>
      </p>
    </div>
  </div>
</BaseLayout>
